info:
  name: get_lists_1
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/lists/:listId
  params:
    - name: listId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
